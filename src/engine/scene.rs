extern crate serde;
extern crate serde_json;


pub struct Scene {
  // entities
  // camera ???
  // skybox - should it be asigned to camera ???
  // lights

}


impl Scene {
  pub fn load(path: &str) -> Scene {
    Scene {}
  }

}